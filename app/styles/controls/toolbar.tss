'.toolbar': {
	bottom: 0,
	width: Ti.UI.FILL
}

'.toolbar[platform=android]': {
	/*
	 * FIXME: `backgroundColor` really exists (at least for android),
	 * but in the docs it is excluded
	 */
	// backgroundColor: 'gray'
}
