'.toolbar': {
	title: 'MyMenu',
	subtitle: 'Subtitle',
	width: Ti.UI.FILL,
	top: 0,
	barColor: 'green',
	/*
	 * FIXME: `displayHomeAsUp` and `homeButtonEnabled` not defined in the docs 
	 */
	// displayHomeAsUp: true,
	// homeButtonEnabled: true
}
